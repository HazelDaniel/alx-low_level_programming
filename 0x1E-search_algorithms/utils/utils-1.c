#include <stdio.h>

/**
 * min - a function that ... figure it out sherlock!
 * @x: the first operand
 * @y: the second operand
 * Return: maximum
 **/
size_t min(size_t x, size_t y)
{
	if (x < y)
		return (x);
	return (y);
}

